name: Build & Deploy The Contour Clinic Richmond Website
permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - 'sites/thecontourclinicrichmond.co.uk/**'
      - '.github/workflows/site-thecontourclinicrichmond.yml'
      - '.github/workflows/site-template.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'sites/thecontourclinicrichmond.co.uk/**'
      - '.github/workflows/site-thecontourclinicrichmond.yml'
      - '.github/workflows/site-template.yml'

jobs:
  thecontourclinicrichmond:
    name: "Build & Deploy thecontourclinicrichmond.co.uk"
    uses: james-d12/Websites/.github/workflows/template/site-template.yml@main
    with:
      site: thecontourclinicrichmond.co.uk
    secrets:
      server_host: ${{ secrets.HOST }}
      server_username: ${{ secrets.USERNAME }}
      server_password: ${{ secrets.PASSWORD }}
      server_ssh_port: ${{ secrets.PORT }}