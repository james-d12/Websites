---
interface Props {
    class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class={`w-full bg-white flex justify-center ${className}`} aria-hidden="true">
    <div class="w-full h-2 bg-linear-to-l from-green-dark via-green-dark to-green-darker"></div>
</div>
