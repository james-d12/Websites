---
interface Props {
    quote: string;
    side: "left" | "right";
    top: string;
    rotate: "-2" | "2";
}

const { quote, side, top, rotate } = Astro.props;
---

<div
    class:list={[
        "hidden min-[2200px]:block absolute w-56 rounded-2xl bg-linear-to-br from-green-dark via-green-dark to-green-darker p-6 text-white shadow-lg",
        side === "left" ? "-left-96" : "-right-96",
        rotate === "-2" ? "-rotate-2" : "rotate-2",
    ]}
    style={`top: ${top}`}
>
    <blockquote class="text-xl italic leading-relaxed">
        &#8220;{quote}&#8221;
    </blockquote>
</div>
